#!/bin/sh

# Script to determine if my computer is connected to the internet

WIFI=$(nmcli -a | awk '/^wlo1/' | awk '{print $2}')
ETH=$(nmcli -a | awk '/^eno1/' | awk '{print $2}')

if [[ "$WIFI" == "connected" ]]; then
	echo "  "
else
	echo "  "
fi	
